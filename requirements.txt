# ── Speech-to-Text WebSocket Server Requirements ──
# Install with: pip install -r requirements.txt

faster-whisper>=1.0.0
websockets>=12.0
numpy>=1.24.0

# Notes:
# - faster-whisper will download the "small" Whisper model (~500MB) on first run.
# - CPU-only setup uses compute_type="int8" (see server/stt_server.py).
# - For GPU acceleration, install a CUDA-enabled PyTorch/CTranslate2 build and
#   change the model line in stt_server.py to device="cuda", compute_type="float16".
